<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+). allowBackup="false" already disables legacy
     backup; these rules additionally exclude app data from cloud backup
     and device-to-device transfer so auth tokens / drafts / PII never
     leave the device through those channels. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
